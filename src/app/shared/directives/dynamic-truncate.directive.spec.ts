import { DynamicTruncateDirective } from './dynamic-truncate.directive';

describe('DynamicTruncateDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicTruncateDirective();
    expect(directive).toBeTruthy();
  });
});
