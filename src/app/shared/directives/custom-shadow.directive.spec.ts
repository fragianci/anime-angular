import { CustomShadowDirective } from './custom-shadow.directive';

describe('CustomShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomShadowDirective();
    expect(directive).toBeTruthy();
  });
});
